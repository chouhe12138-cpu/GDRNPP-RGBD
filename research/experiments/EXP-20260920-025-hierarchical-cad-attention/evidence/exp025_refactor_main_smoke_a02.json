{
  "kind": "derived_summary",
  "source": "output/diagnostics/exp025_refactor_main_smoke_a02",
  "command": "main_gdrn.py --config-file smoke.py --num-gpus 1 --opts OUTPUT_DIR=... MODEL.POSE_NET.XYZ_RENDERER=cpp",
  "note": "real entry after the v2 refactor; smoke.py now carries the local shape itself (IMS_PER_BATCH=4, REFERENCE_BS=48, accumulation 12) instead of inheriting the formal config, and the 55-ROI smoke split gives 13 iterations per epoch",
  "exit_code": 0,
  "epoch1": {
    "iteration": 12,
    "epoch": 1,
    "iters_per_epoch": 13,
    "optimizer_steps": [
      2
    ],
    "optimizer_lrs": [
      2.9999999999999997e-06
    ],
    "scheduler_last_epoch": 2,
    "gradscaler_scale": 65536.0,
    "gradscaler_growth_tracker": 2
  },
  "final_loss": "total_loss 13.71 (13.72); loss_cad_t1 2.063, t2 4.166, t3 6.425, residual 0.1501, mask 0.8569 at iter 12/13",
  "checkpoint": "checkpoints/model_epoch_001.pth",
  "interpretation": "the engine still lands optimizer updates on the accumulation boundary and the truncated epoch window; the scale stayed at 65536 with no skip, whereas the single-step real_smoke overflows at 65536 -- the accumulation divisor shrinks each micro-batch's scaled fp16 gradient by 12, so formal 48/48 (accumulate=1) sits closer to the fp16 boundary than these local 4x12 diagnostics"
}