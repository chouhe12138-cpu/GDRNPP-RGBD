# EXP013B 结构与证据边界

EXP013B 完全继承 A，只在残差支路的 `16×16` 特征上加入九邻域注意力。关系描述固定为 `Δu、Δv、Δx、Δy、Δz、||ΔXYZ||`，mask 外和图像边界外邻居在 softmax 前失效，注意力残差系数初值为 `0.1`。

B 必须先通过相对 EXP012 的正式门槛，再按预注册规则与 A 比较。因此 B−A 只解释局部几何注意力，不解释独立 XYZ 路径本身。
