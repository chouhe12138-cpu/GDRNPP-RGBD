_base_ = ["../../_base_/lmo_gt_eval.py"]

EXPERIMENT_ID = "EXP-REPLACE-BEFORE-USE"
OUTPUT_DIR = "output/experiments/EXP-REPLACE-BEFORE-USE/RUN-SET-BY-LAUNCHER"

# MODEL.WEIGHTS must be supplied as an explicitly indexed checkpoint. Do not
# infer it from model_final.pth or a directory name.
